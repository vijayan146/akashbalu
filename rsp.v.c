#include <stdio.h>

void main()

{

    int a,b,num;
    printf("enter the a,b value");
    scanf("%d%d",&a,&b);
    {
        for(i=a;i<=b,i++)
        {
            if(i%2==1)
            printf("print the i value%d",i);
        }
    }

}
